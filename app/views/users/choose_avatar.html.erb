<div class="container-fluid d-flex flex-column align-items-center justify-content-center">

  <h1 class="text-center text-primary letter_title py-5">
    <i class="fa-solid fa-circle-1"></i> Choose your avatar
  </h1>

  <div data-controller="avatar">
    <div class="shadow py-5 px-5 border-image letter-bg d-flex gap-5 justify-content-center">
      <%= image_tag "profile_placeholder.jpg", width: 175, height: 175, class: "rounded-circle avatar-item", data: { action: "click->avatar#choose", avatar_target: "img", avatar_path: "profile_placeholder.jpg" } %>
      <%= image_tag "zazu1.png", width: 175, height: 175, class: "rounded-circle avatar-item", data: { action: "click->avatar#choose", avatar_target: "img", avatar_path: "zazu1.png" } %>
      <%= image_tag "mushu1.png", width: 175, height: 175, class: "rounded-circle avatar-item", data: { action: "click->avatar#choose", avatar_target: "img", avatar_path: "mushu1.png" } %>
      <%= image_tag "piolin1.jpg", width: 175, height: 175, class: "rounded-circle avatar-item", data: { action: "click->avatar#choose", avatar_target: "img", avatar_path: "piolin1.jpg" } %>
    </div>

    <%= simple_form_for(current_user, url: upload_avatar_user_path(current_user), html: { method: :patch }) do |f| %>
      <div class="form-inputs">
        <%= f.input :avatar_file, as: :hidden, autofocus: true, input_html: { data: { avatar_target: "input" } } %>
      </div>

      <div class="form-actions text-center pt-5">
        <button type="submit" class="btn-main">
          Next <i class="ms-2 fa-solid fa-arrow-right"></i>
        </button>
      </div>
    <% end %>
 </div>
</div>
